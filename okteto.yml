build:
  # client:
  #   context: ./apps/client
  # server:
  #   context: .
  #   dockerfile: ./apps/server/Dockerfile
deploy:
  - name: Deploy Via Helm
    command: helm repo add bitnami https://charts.bitnami.com/bitnami; helm dep build ./helm; helm install deadmansswitch ./helm;
