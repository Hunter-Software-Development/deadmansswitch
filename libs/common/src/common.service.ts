import { Injectable } from '@nestjs/common';

/**
 * Common Service
 *
 * Currently does nothing, but potentially common functions live here - for future
 */
@Injectable()
export class CommonService {}
